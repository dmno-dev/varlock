---
varlock: minor
---

Add proxy support for Apple container sandboxes: `proxy start --bind` to expose the proxy on a sandbox network gateway, and `proxy env --format apple-container` to emit ready-to-run container wiring.
