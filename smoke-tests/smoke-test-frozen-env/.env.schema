# @defaultSensitive=false
# @currentEnv=$APP_ENV
# ---

# @type=enum(development,production)
APP_ENV=development

PUBLIC_VAR=public-value

# @sensitive
SECRET_TOKEN=dev-token

# @type=boolean
COERCED_FLAG=false
