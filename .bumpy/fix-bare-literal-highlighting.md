---
env-spec-language: patch
---

Improve syntax highlighting for array/object literals: highlight bare item-value literals (e.g. `KEY=[a, b]`, `KEY={ k=v }`) and fix unindented (column-0) comments inside multi-line `fn(...)` literals
