---
varlock: minor
---

New `domainFromUrl()` function to extract the host from a URL, with `registrable=true` to narrow it to the registrable domain, and a new `allowIpV6` option on the `domain` type
