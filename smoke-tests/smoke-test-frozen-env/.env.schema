# @defaultSensitive=false
# @currentEnv=$APP_ENV
# ---

# @type=enum(development,production)
APP_ENV=development

PUBLIC_VAR=public-value

# @sensitive
SECRET_TOKEN=dev-token

# @type=boolean
COERCED_FLAG=false

# declared but unset: the shape of the REDIS_URL incident, where an operator
# supplies a value at boot that the seal does not have
# @optional
UNSET_IN_SEAL=
