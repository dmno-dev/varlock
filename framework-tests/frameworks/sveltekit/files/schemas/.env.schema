# @defaultSensitive=false @defaultRequired=infer @defaultDynamic=inferFromSensitive
# @currentEnv=$APP_ENV
# ---

# @type=enum(dev, prod)
APP_ENV=dev

PUBLIC_VAR=public-test-value
API_URL=https://api.example.com

# @dynamic
PUBLIC_DYNAMIC_VAR=public-dynamic-default

# @sensitive
SECRET_KEY=super-secret-value
