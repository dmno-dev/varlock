---
varlock: patch
---

Update gunshi to 0.35. `varlock cache status`/`clear` are now proper subcommands with scoped help and completion, and `printenv`/`explain`/`reveal`/`scan`/`audit` now declare their positional arguments so they appear in `--help` and shell completion.
