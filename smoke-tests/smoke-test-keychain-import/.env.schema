# @defaultSensitive=false
# ---

# @sensitive
OLD_API_KEY=keychain(service="varlock-smoke-test-import", account="smoke-test-keychain-import:local:OLD_API_KEY")

# @sensitive
OLD_DATABASE_URL=keychain(service="varlock-smoke-test-import", account="smoke-test-keychain-import:local:OLD_DATABASE_URL")
