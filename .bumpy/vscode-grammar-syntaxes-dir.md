---
env-spec-language: patch
---

Move the TextMate grammar and language configuration from `language/` to `syntaxes/` (the standard VS Code extension layout, and the location GitHub Linguist's grammar compiler expects).
