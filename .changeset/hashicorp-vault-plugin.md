---
"@varlock/hashicorp-vault-plugin": patch
"varlock": patch
---

New plugin for loading secrets from HashiCorp Vault (KV v2 secrets engine) and OpenBao. Also makes `validate` optional on data type definitions.
