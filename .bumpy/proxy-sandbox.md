---
varlock: minor
---

Add `proxy run --sandbox` — a built-in macOS credential + egress jail that forces agent egress through the proxy and blocks direct access to secrets, closing the same-uid escape.
